# Supabase — required for auth, waitlist, feedback, LFG persistence
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Site URL — used for auth redirect URLs (defaults to window.location.origin)
# Set to your production domain when hosting
NEXT_PUBLIC_SITE_URL=
